@model Talent

@{
    ViewData["Title"] = "Delete Talent";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="glass-card" style="border-color: rgba(255, 0, 85, 0.3);">
                <div class="card-header border-0 py-4" style="background: rgba(255, 0, 85, 0.1);">
                    <h3 class="mb-0" style="color: var(--danger-color);">
                        <i class="bi bi-exclamation-triangle me-2"></i>REMOVE SKILL
                    </h3>
                </div>
                <div class="card-body p-4">
                    <div class="alert" style="background: rgba(255, 0, 85, 0.1); border: 1px solid rgba(255, 0, 85, 0.3); border-radius: 12px;">
                        <strong style="color: var(--danger-color);">WARNING!</strong> <span class="text-secondary">This action cannot be undone. Are you sure you want to delete this skill?</span>
                    </div>

                    <div class="mb-4">
                        <h5 style="color: var(--danger-color);">Skill Details:</h5>
                        <dl class="row">
                            <dt class="col-sm-4 text-secondary">Skill Name:</dt>
                            <dd class="col-sm-8 text-white">@Model.TalentName</dd>

                            <dt class="col-sm-4 text-secondary">Description:</dt>
                            <dd class="col-sm-8 text-secondary">@Model.Description</dd>

                            <dt class="col-sm-4 text-secondary">Proficiency:</dt>
                            <dd class="col-sm-8">
                                <span class="badge" style="background: rgba(0, 255, 136, 0.2); color: var(--neon-green);">@Model.ProficiencyLevel</span>
                            </dd>

                            <dt class="col-sm-4 text-secondary">Skill ID:</dt>
                            <dd class="col-sm-8"><code style="color: var(--neon-cyan);">@Model.TalentId</code></dd>
                        </dl>
                    </div>

                    <form asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@Model.TalentId" />
                        
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn" style="background: rgba(160, 160, 192, 0.2); color: var(--text-secondary); border: 1px solid rgba(160, 160, 192, 0.3);">
                                <i class="bi bi-x-circle me-1"></i>Cancel
                            </a>
                            <button type="submit" class="btn" style="background: var(--danger-color); color: #fff;">
                                <i class="bi bi-trash me-1"></i>Remove
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .glass-card {
            background: rgba(10, 10, 20, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 0, 85, 0.3);
            border-radius: 16px;
            overflow: hidden;
        }
    </style>
}
